The audit-trail unit: verdict first, evidence on expand.

```jsx
<ProofBlock title="Randomness" verdict="verified" summary="Chainlink VRF request and fulfilment bound to epoch 42.">
  <EvidenceRow label="Request id" value="0x9f2c…c1" verified />
  <EvidenceRow label="Fulfilment tx" value="0x41ba…07" verified href="…" />
</ProofBlock>
```

Collapse by default on mobile. Use `footnote` for the verification limitation on the block that reports the winner.