The fixed 16 slots, 4x4 — the brand SLOTS mark as live data.

```jsx
<SlotGrid slots={["mine","filled","filled","empty", …]} legend />
<SlotGrid slots={pool} size="sm" caption={null} />
```

Always 16 cells. `drawn` marks the winning slot after finalization and is the only teal cell. Never label a cell with an amount.