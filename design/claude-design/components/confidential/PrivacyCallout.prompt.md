The public/private boundary. Use once per surface, near the first confidential value.

```jsx
<PrivacyCallout />
<PrivacyCallout compact limitation={null} />
```

Defaults carry the approved copy. Never edit it to claim anonymity, hidden addresses or private transaction timing.