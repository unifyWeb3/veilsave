Frame for repeated records, dialog bodies and framed tools.

```jsx
<Card tone="base" pad={20}>…</Card>
<Card tone="private">…</Card>
```

Do not card a page section (use SectionHead + hairlines) and never nest cards. Radius stays 8px.