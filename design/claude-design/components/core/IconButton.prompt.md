Icon-only control for row affordances (copy, explorer, hide).

```jsx
<IconButton name="copy" label="Copy transaction hash" />
<IconButton name="eye-off" label="Hide balance" tone="private" />
```

`label` is mandatory. Never the only route to an action on mobile — pair with a text control when it carries recovery.