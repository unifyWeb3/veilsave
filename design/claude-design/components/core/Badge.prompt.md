Static identity metadata.

```jsx
<Badge tone="neutral" icon="grid-2x2">Slot 07</Badge>
<Badge mono>EPOCH 42</Badge>
```

Use for network, epoch, slot, contract labels. Live protocol state belongs in StatusPill.