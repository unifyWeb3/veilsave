Dropdown for public filters.

```jsx
<Select label="Range" value={r} onChange={setR} options={[{value:"30",label:"Last 30 epochs"},{value:"all",label:"All epochs"}]} />
```

Native chrome on purpose: it is keyboard- and screen-reader-correct for free.