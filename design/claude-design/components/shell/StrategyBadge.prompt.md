Strategy identity — required wherever prize funding is implied.

```jsx
<StrategyBadge mode="test" />
<StrategyBadge mode="live" withHint={false} />
```

TEST YIELD is amber because it is a caution, not a feature. Never place a rate, APY or projection next to it.