The lockup.

```jsx
<Wordmark mark size={16} />
<Wordmark size={20} tone="muted" />
```

Type-set, not an image: no vector mark was supplied. `veil` sits at 500 in muted ink, `Save` at 600 in primary — the emphasis is on what the user keeps.